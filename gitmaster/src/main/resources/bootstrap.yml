server:
  port: 8090
  tomcat:
    uri-encoding: UTF-8
spring:
  application:
    name: ware-elastic-search
  data:
    elasticsearch:
      # 默认 elasticsearch
      cluster-name: elasticsearch
      # 9200作为Http协议，主要用于外部通讯
      # 9300作为Tcp协议，jar之间就是通过tcp协议通讯
      #cluster-nodes: 192.168.184.105:9300
      cluster-nodes: 127.0.0.1:9300


